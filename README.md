//Se nao tenho o projeto 
1- npm init -y
2- npm install express

//Se ja tenho o projeto
1- git clone (aqui o link do github)
2- abrir projeto no VS Code
3- verificar se tem server.js, package.json e package-lock.json
4- npm install pra criar a pasta node_modules
5- no gitbash dentro de Desktop/api executo: npm run start

//Pra salvar
git add .
git commit -m "mensagem"
git push origin main

//Se tiver uma conta no computador 
Vai no gerenciador de credenciais, credenciais do windows e remove quem tiver no github